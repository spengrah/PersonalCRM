import XCTest
@testable import CRMMacCore

final class PlaceholderTests: XCTestCase {
    func testTargetCompiles() {
        // Sentinel so the test target has at least one method until
        // the real tests land in the next commit.
        XCTAssertTrue(true)
    }
}
