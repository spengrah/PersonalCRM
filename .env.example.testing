# Testing Environment - Ultra-fast cadences for immediate validation
CRM_ENV=testing
DATABASE_URL=postgres://crm_user:crm_password@localhost:5432/crm_test?sslmode=disable
GIN_MODE=debug

# Database settings for testing
POSTGRES_USER=crm_user
POSTGRES_PASSWORD=crm_password
POSTGRES_DB=crm_test
POSTGRES_PORT=5432

# Testing-specific settings
LOG_LEVEL=debug
SCHEDULER_ENABLED=true
SCHEDULER_CRON="*/30s"  # Run every 30 seconds for testing

# Reminder cadence overrides (handled in code based on CRM_ENV)
# Weekly = 2 minutes
# Monthly = 10 minutes  
# Quarterly = 30 minutes
# Biannual = 1 hour
# Annual = 2 hours
