# DRAFT labels for the CON-042 judge residue (the one clearly judge-only item:
# "a confirmation prompt warns the action cannot be undone"). NOT ground truth.
#
# This is a STRUCTURAL placeholder. The real draft — genuine critiques from a
# stronger model than the runtime judge — is produced by a MANUAL authoring run
# (`QA_LABELER=<stronger-profile> bun run tests/tours/judge/label.ts`, needs
# codex quota) and committed like the corpus captures. The maintainer then
# CORRECTS this draft in place into CON-042.labeled.yaml (flipping status to
# human-confirmed), without re-running anything. Merge does NOT depend on either
# step — see judge/DEFERRED.md.
case_id: CON-042-clean
behavior_id: CON-042
drafted_by: PENDING-manual-stronger-model-run
note: >-
  DRAFT — placeholder pending the manual stronger-model authoring run and the
  maintainer's correction. NOT ground truth and NOT a merge gate.
items:
  - then_index: 0
    draft_verdict: unsure
    draft_critique: >-
      PENDING: the delete confirm dialog message (dialogs[].message = "Are you
      sure you want to delete this contact? This action cannot be undone.") must
      be judged for whether it warns of irreversibility. Fill via the labeling
      CLI, then correct.
    status: draft
