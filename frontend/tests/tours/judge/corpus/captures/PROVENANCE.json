{
  "seedProfile": "prod-shaped",
  "captureGeneratorVersion": 1,
  "sourceRun": "local-20260714T151954Z",
  "note": "Base capture fixtures for the 3 first-cut tours (contacts, dashboard, cadence-followup), curated from a LOCAL prod-shaped sweep (crm-admin --reset-and-seed --profile prod-shaped) against a CRM_ENV=testing localhost stack (docker Postgres + go run ./cmd/crm-api, accelerated testing frame). The committed corpus always comes from the local reset (staging is reachable from the QA sandbox but is NOT the corpus source). This regen (post seed-fidelity arc, develop at 646bbf1f) is the first over the causally-seeded world: outbound/mutual message directions, non-collapsed contact_by, lifecycle-driven cadence_due tasks, and assertion-backed knowledge caches all come from real provider replay, and the cadence tour's new pending-reply capture (003) shifted the numbering of the later cadence-followup captures. Screenshots recorded by the sweep are stripped at curation (the committed corpus stays aria-only). Every capture is UUID-mapped + host-redacted by the tours normalizer, then run through judge/corpus/scrub.ts (email/phone -> <email:N>/<phone:N>). Provenance is proven MECHANICALLY: every contact name carries the synth-prodshaped- factory prefix (asserted by judge/corpus/pii-audit.ts), so a real target's un-prefixed names would fail the audit regardless of this label."
}
