# Frontend Environment Configuration
# Copy this file to .env.local for local development

# API Authentication
# Must match the API_KEY in backend .env
# Generate with: openssl rand -hex 32
NEXT_PUBLIC_API_KEY=your-api-key-here
