// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: telegram_chat_config.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const DeleteTelegramChatConfig = `-- name: DeleteTelegramChatConfig :exec
DELETE FROM telegram_chat_config WHERE telegram_chat_id = $1
`

func (q *Queries) DeleteTelegramChatConfig(ctx context.Context, telegramChatID int64) error {
	_, err := q.db.Exec(ctx, DeleteTelegramChatConfig, telegramChatID)
	return err
}

const GetTelegramChatConfig = `-- name: GetTelegramChatConfig :one
SELECT id, telegram_chat_id, chat_title, chat_type, member_count, status, backfill_cursor, backfill_complete, created_at, updated_at FROM telegram_chat_config WHERE telegram_chat_id = $1
`

func (q *Queries) GetTelegramChatConfig(ctx context.Context, telegramChatID int64) (*TelegramChatConfig, error) {
	row := q.db.QueryRow(ctx, GetTelegramChatConfig, telegramChatID)
	var i TelegramChatConfig
	err := row.Scan(
		&i.ID,
		&i.TelegramChatID,
		&i.ChatTitle,
		&i.ChatType,
		&i.MemberCount,
		&i.Status,
		&i.BackfillCursor,
		&i.BackfillComplete,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return &i, err
}

const ListTelegramChatConfigs = `-- name: ListTelegramChatConfigs :many
SELECT id, telegram_chat_id, chat_title, chat_type, member_count, status, backfill_cursor, backfill_complete, created_at, updated_at FROM telegram_chat_config ORDER BY chat_title
`

func (q *Queries) ListTelegramChatConfigs(ctx context.Context) ([]*TelegramChatConfig, error) {
	rows, err := q.db.Query(ctx, ListTelegramChatConfigs)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []*TelegramChatConfig{}
	for rows.Next() {
		var i TelegramChatConfig
		if err := rows.Scan(
			&i.ID,
			&i.TelegramChatID,
			&i.ChatTitle,
			&i.ChatType,
			&i.MemberCount,
			&i.Status,
			&i.BackfillCursor,
			&i.BackfillComplete,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, &i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const UpdateTelegramChatConfigStatus = `-- name: UpdateTelegramChatConfigStatus :one
UPDATE telegram_chat_config
SET status = $1, updated_at = NOW()
WHERE telegram_chat_id = $2
RETURNING id, telegram_chat_id, chat_title, chat_type, member_count, status, backfill_cursor, backfill_complete, created_at, updated_at
`

type UpdateTelegramChatConfigStatusParams struct {
	Status         string `json:"status"`
	TelegramChatID int64  `json:"telegram_chat_id"`
}

func (q *Queries) UpdateTelegramChatConfigStatus(ctx context.Context, arg UpdateTelegramChatConfigStatusParams) (*TelegramChatConfig, error) {
	row := q.db.QueryRow(ctx, UpdateTelegramChatConfigStatus, arg.Status, arg.TelegramChatID)
	var i TelegramChatConfig
	err := row.Scan(
		&i.ID,
		&i.TelegramChatID,
		&i.ChatTitle,
		&i.ChatType,
		&i.MemberCount,
		&i.Status,
		&i.BackfillCursor,
		&i.BackfillComplete,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return &i, err
}

const UpsertTelegramChatConfig = `-- name: UpsertTelegramChatConfig :one
INSERT INTO telegram_chat_config (
    telegram_chat_id, chat_title, chat_type, member_count, status
) VALUES (
    $1, $2, $3, $4, $5
)
ON CONFLICT (telegram_chat_id) DO UPDATE SET
    chat_title = COALESCE(EXCLUDED.chat_title, telegram_chat_config.chat_title),
    member_count = COALESCE(EXCLUDED.member_count, telegram_chat_config.member_count),
    status = telegram_chat_config.status,
    updated_at = NOW()
RETURNING id, telegram_chat_id, chat_title, chat_type, member_count, status, backfill_cursor, backfill_complete, created_at, updated_at
`

type UpsertTelegramChatConfigParams struct {
	TelegramChatID int64       `json:"telegram_chat_id"`
	ChatTitle      pgtype.Text `json:"chat_title"`
	ChatType       string      `json:"chat_type"`
	MemberCount    pgtype.Int4 `json:"member_count"`
	Status         string      `json:"status"`
}

func (q *Queries) UpsertTelegramChatConfig(ctx context.Context, arg UpsertTelegramChatConfigParams) (*TelegramChatConfig, error) {
	row := q.db.QueryRow(ctx, UpsertTelegramChatConfig,
		arg.TelegramChatID,
		arg.ChatTitle,
		arg.ChatType,
		arg.MemberCount,
		arg.Status,
	)
	var i TelegramChatConfig
	err := row.Scan(
		&i.ID,
		&i.TelegramChatID,
		&i.ChatTitle,
		&i.ChatType,
		&i.MemberCount,
		&i.Status,
		&i.BackfillCursor,
		&i.BackfillComplete,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return &i, err
}
