// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: job_sample.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const InsertJobExecSample = `-- name: InsertJobExecSample :exec

INSERT INTO job_exec_sample (river_job_id, kind, queue, attempted_at, finalized_at, attempt, state, queue_wait_ms, created_at)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)
ON CONFLICT (river_job_id, attempt, attempted_at) DO NOTHING
`

type InsertJobExecSampleParams struct {
	RiverJobID  int64              `json:"river_job_id"`
	Kind        string             `json:"kind"`
	Queue       string             `json:"queue"`
	AttemptedAt pgtype.Timestamptz `json:"attempted_at"`
	FinalizedAt pgtype.Timestamptz `json:"finalized_at"`
	Attempt     int32              `json:"attempt"`
	State       string             `json:"state"`
	QueueWaitMs int64              `json:"queue_wait_ms"`
	CreatedAt   pgtype.Timestamptz `json:"created_at"`
}

// Queries over job_exec_sample (the River job-execution sampling table) plus
// the Tier-0 one-shot read over live river_job.
//
// Convention (mirrors health.sql / sync_staleness.sql): every timestamp is
// supplied by the caller from accelerated time — these queries NEVER call
// NOW(). Analyst decision queries take @window_start/@window_end; the trim + Tier-0 take an
// accelerated @cutoff. This keeps insert-time (job_exec_sample.created_at) and
// trim-time on the same clock under time acceleration.
// Plain per-event insert (no tx). created_at is supplied explicitly from
// accelerated time (no DEFAULT NOW()). ON CONFLICT dedups a re-delivered
// Subscribe event so it cannot double-count and skew the aggregates, without
// erroring the writer.
func (q *Queries) InsertJobExecSample(ctx context.Context, arg InsertJobExecSampleParams) error {
	_, err := q.db.Exec(ctx, InsertJobExecSample,
		arg.RiverJobID,
		arg.Kind,
		arg.Queue,
		arg.AttemptedAt,
		arg.FinalizedAt,
		arg.Attempt,
		arg.State,
		arg.QueueWaitMs,
		arg.CreatedAt,
	)
	return err
}

const JobExecMaxConcurrency = `-- name: JobExecMaxConcurrency :one
WITH ov AS (
    SELECT GREATEST(attempted_at, $1::timestamptz) AS start_t,
           LEAST(finalized_at,  $2::timestamptz)    AS end_t
    FROM job_exec_sample
    WHERE attempted_at <= $2::timestamptz AND finalized_at >= $1::timestamptz
),
endpoints AS (
    SELECT start_t AS t,  1 AS delta FROM ov
    UNION ALL
    SELECT end_t   AS t, -1 AS delta FROM ov
),
netted AS ( SELECT t, SUM(delta) AS d FROM endpoints GROUP BY t ),
running AS ( SELECT t, SUM(d) OVER (ORDER BY t) AS c FROM netted )
SELECT COALESCE(MAX(c), 0)::int AS max_concurrency FROM running
`

type JobExecMaxConcurrencyParams struct {
	WindowStart pgtype.Timestamptz `json:"window_start"`
	WindowEnd   pgtype.Timestamptz `json:"window_end"`
}

// Metric 1: peak concurrency over [@window_start,@window_end]. Intervals are selected by
// OVERLAP with the window and clamped to it (so a job spanning the whole window
// still contributes); deltas are netted per timestamp before the running sum
// (so a release and an acquire at the same instant cancel — no false peak).
func (q *Queries) JobExecMaxConcurrency(ctx context.Context, arg JobExecMaxConcurrencyParams) (int32, error) {
	row := q.db.QueryRow(ctx, JobExecMaxConcurrency, arg.WindowStart, arg.WindowEnd)
	var max_concurrency int32
	err := row.Scan(&max_concurrency)
	return max_concurrency, err
}

const JobExecRunDurationByKind = `-- name: JobExecRunDurationByKind :many
SELECT kind,
       COUNT(*)::bigint AS n,
       percentile_cont(0.5)  WITHIN GROUP (ORDER BY EXTRACT(EPOCH FROM (finalized_at - attempted_at)))::float8 AS p50_run_s,
       percentile_cont(0.95) WITHIN GROUP (ORDER BY EXTRACT(EPOCH FROM (finalized_at - attempted_at)))::float8 AS p95_run_s,
       MAX(EXTRACT(EPOCH FROM (finalized_at - attempted_at)))::float8 AS max_run_s
FROM job_exec_sample
WHERE attempted_at >= $1::timestamptz AND attempted_at <= $2::timestamptz
GROUP BY kind
ORDER BY kind
`

type JobExecRunDurationByKindParams struct {
	WindowStart pgtype.Timestamptz `json:"window_start"`
	WindowEnd   pgtype.Timestamptz `json:"window_end"`
}

type JobExecRunDurationByKindRow struct {
	Kind    string  `json:"kind"`
	N       int64   `json:"n"`
	P50RunS float64 `json:"p50_run_s"`
	P95RunS float64 `json:"p95_run_s"`
	MaxRunS float64 `json:"max_run_s"`
}

// Metric 3: run duration percentiles per kind over [@window_start,@window_end].
func (q *Queries) JobExecRunDurationByKind(ctx context.Context, arg JobExecRunDurationByKindParams) ([]*JobExecRunDurationByKindRow, error) {
	rows, err := q.db.Query(ctx, JobExecRunDurationByKind, arg.WindowStart, arg.WindowEnd)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []*JobExecRunDurationByKindRow{}
	for rows.Next() {
		var i JobExecRunDurationByKindRow
		if err := rows.Scan(
			&i.Kind,
			&i.N,
			&i.P50RunS,
			&i.P95RunS,
			&i.MaxRunS,
		); err != nil {
			return nil, err
		}
		items = append(items, &i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const JobExecSaturatedSeconds = `-- name: JobExecSaturatedSeconds :one
WITH ov AS (
    SELECT GREATEST(attempted_at, $2::timestamptz) AS start_t,
           LEAST(finalized_at,  $3::timestamptz)    AS end_t
    FROM job_exec_sample
    WHERE attempted_at <= $3::timestamptz AND finalized_at >= $2::timestamptz
),
endpoints AS (
    SELECT start_t AS t,  1 AS delta FROM ov
    UNION ALL
    SELECT end_t   AS t, -1 AS delta FROM ov
),
netted AS ( SELECT t, SUM(delta) AS d FROM endpoints GROUP BY t ),
running AS ( SELECT t, SUM(d) OVER (ORDER BY t) AS c FROM netted ),
seg AS ( SELECT t, c, LEAD(t) OVER (ORDER BY t) AS next_t FROM running )
SELECT COALESCE(EXTRACT(EPOCH FROM SUM(next_t - t) FILTER (WHERE c >= $1::int)), 0)::float8
       AS saturated_seconds
FROM seg WHERE next_t IS NOT NULL
`

type JobExecSaturatedSecondsParams struct {
	Threshold   int32              `json:"threshold"`
	WindowStart pgtype.Timestamptz `json:"window_start"`
	WindowEnd   pgtype.Timestamptz `json:"window_end"`
}

// Metric 1: total wall-seconds spent at concurrency >= @threshold (pass
// MaxWorkers). Same netted, window-clipped timeline as JobExecMaxConcurrency.
func (q *Queries) JobExecSaturatedSeconds(ctx context.Context, arg JobExecSaturatedSecondsParams) (float64, error) {
	row := q.db.QueryRow(ctx, JobExecSaturatedSeconds, arg.Threshold, arg.WindowStart, arg.WindowEnd)
	var saturated_seconds float64
	err := row.Scan(&saturated_seconds)
	return saturated_seconds, err
}

const JobExecWaitByKind = `-- name: JobExecWaitByKind :many
SELECT kind,
       COUNT(*)::bigint AS n,
       percentile_cont(0.5)  WITHIN GROUP (ORDER BY queue_wait_ms / 1000.0)::float8 AS p50_wait_s,
       percentile_cont(0.95) WITHIN GROUP (ORDER BY queue_wait_ms / 1000.0)::float8 AS p95_wait_s,
       (MAX(queue_wait_ms) / 1000.0)::float8 AS max_wait_s
FROM job_exec_sample
WHERE attempted_at >= $1::timestamptz AND attempted_at <= $2::timestamptz
GROUP BY kind
ORDER BY kind
`

type JobExecWaitByKindParams struct {
	WindowStart pgtype.Timestamptz `json:"window_start"`
	WindowEnd   pgtype.Timestamptz `json:"window_end"`
}

type JobExecWaitByKindRow struct {
	Kind     string  `json:"kind"`
	N        int64   `json:"n"`
	P50WaitS float64 `json:"p50_wait_s"`
	P95WaitS float64 `json:"p95_wait_s"`
	MaxWaitS float64 `json:"max_wait_s"`
}

// Metric 2 (context): overall wait percentiles per kind, from the stored
// queue_wait_ms (River's exact QueueWaitDuration — NOT a timestamp subtraction).
func (q *Queries) JobExecWaitByKind(ctx context.Context, arg JobExecWaitByKindParams) ([]*JobExecWaitByKindRow, error) {
	rows, err := q.db.Query(ctx, JobExecWaitByKind, arg.WindowStart, arg.WindowEnd)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []*JobExecWaitByKindRow{}
	for rows.Next() {
		var i JobExecWaitByKindRow
		if err := rows.Scan(
			&i.Kind,
			&i.N,
			&i.P50WaitS,
			&i.P95WaitS,
			&i.MaxWaitS,
		); err != nil {
			return nil, err
		}
		items = append(items, &i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const JobExecWaitDuringSaturationByKind = `-- name: JobExecWaitDuringSaturationByKind :many
WITH ov AS (
    SELECT GREATEST(attempted_at, $1::timestamptz) AS start_t, LEAST(finalized_at, $2::timestamptz) AS end_t
    FROM job_exec_sample WHERE attempted_at <= $2::timestamptz AND finalized_at >= $1::timestamptz
),
endpoints AS ( SELECT start_t AS t, 1 AS delta FROM ov UNION ALL SELECT end_t, -1 FROM ov ),
netted   AS ( SELECT t, SUM(delta) AS d FROM endpoints GROUP BY t ),
running  AS ( SELECT t, SUM(d) OVER (ORDER BY t) AS c FROM netted ),
seg      AS ( SELECT t AS seg_start, LEAD(t) OVER (ORDER BY t) AS seg_end, c FROM running ),
sat      AS ( SELECT seg_start, seg_end FROM seg WHERE seg_end IS NOT NULL AND c >= $3::int ),
waiters  AS (
    SELECT id, kind, queue_wait_ms, attempted_at,
           attempted_at - make_interval(secs => queue_wait_ms / 1000.0) AS eligible_at,
           GREATEST(attempted_at - make_interval(secs => queue_wait_ms / 1000.0), $1::timestamptz) AS clip_start,
           LEAST(attempted_at, $2::timestamptz) AS clip_end
    FROM job_exec_sample
    WHERE attempted_at >= $1::timestamptz AND attempted_at <= $2::timestamptz AND queue_wait_ms > 0
),
wait_sat AS (
    SELECT w.id, w.kind, w.queue_wait_ms,
           GREATEST(EXTRACT(EPOCH FROM (w.clip_end - w.clip_start)), 0) AS waited_in_window_s,
           -- CASE guards the LEFT JOIN no-match row: LEAST/GREATEST ignore NULLs,
           -- so LEAST(clip_end, NULL) - GREATEST(clip_start, NULL) would wrongly
           -- yield the FULL wait window for a waiter that overlapped NO saturated
           -- segment. Force 0 in that case so saturated_wait_s ⊆ waited_in_window_s.
           COALESCE(SUM(
               CASE WHEN s.seg_start IS NULL THEN 0
                    ELSE EXTRACT(EPOCH FROM (LEAST(w.clip_end, s.seg_end) - GREATEST(w.clip_start, s.seg_start)))
               END), 0) AS sat_wait_s
    FROM waiters w
    LEFT JOIN sat s ON s.seg_start < w.clip_end AND s.seg_end > w.clip_start
    GROUP BY w.id, w.kind, w.queue_wait_ms, w.clip_start, w.clip_end
)
SELECT kind,
       COUNT(*)::bigint AS n_waiters,
       SUM(queue_wait_ms / 1000.0)::float8 AS total_wait_s,
       SUM(waited_in_window_s)::float8     AS waited_in_window_s,
       SUM(sat_wait_s)::float8             AS saturated_wait_s,
       percentile_cont(0.95) WITHIN GROUP (ORDER BY queue_wait_ms / 1000.0)::float8 AS p95_wait_s
FROM wait_sat
GROUP BY kind
ORDER BY kind
`

type JobExecWaitDuringSaturationByKindParams struct {
	WindowStart pgtype.Timestamptz `json:"window_start"`
	WindowEnd   pgtype.Timestamptz `json:"window_end"`
	Threshold   int32              `json:"threshold"`
}

type JobExecWaitDuringSaturationByKindRow struct {
	Kind            string  `json:"kind"`
	NWaiters        int64   `json:"n_waiters"`
	TotalWaitS      float64 `json:"total_wait_s"`
	WaitedInWindowS float64 `json:"waited_in_window_s"`
	SaturatedWaitS  float64 `json:"saturated_wait_s"`
	P95WaitS        float64 `json:"p95_wait_s"`
}

// THE gate query for metric 2. For each waiter, its wait window is
// [eligible_at, attempted_at] where eligible_at = attempted_at - queue_wait
// (robust to retry scheduled_at mutation). Reports, per kind, the wait DURATION
// that overlapped a SATURATED segment (c >= @threshold) vs. window-clipped
// denominators. Denominators (all consistent, window-clipped so a
// boundary-spanning wait can't understate the ratio): waited_in_window_s = the
// wait window clipped to [@window_start,@window_end]; saturated_wait_s ⊆ waited_in_window_s
// (both window-clipped). total_wait_s is the FULL per-attempt queue_wait for
// context only (may exceed the window). Use saturated_wait_s / waited_in_window_s
// as the "waits because the pool is full" ratio.
func (q *Queries) JobExecWaitDuringSaturationByKind(ctx context.Context, arg JobExecWaitDuringSaturationByKindParams) ([]*JobExecWaitDuringSaturationByKindRow, error) {
	rows, err := q.db.Query(ctx, JobExecWaitDuringSaturationByKind, arg.WindowStart, arg.WindowEnd, arg.Threshold)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []*JobExecWaitDuringSaturationByKindRow{}
	for rows.Next() {
		var i JobExecWaitDuringSaturationByKindRow
		if err := rows.Scan(
			&i.Kind,
			&i.NWaiters,
			&i.TotalWaitS,
			&i.WaitedInWindowS,
			&i.SaturatedWaitS,
			&i.P95WaitS,
		); err != nil {
			return nil, err
		}
		items = append(items, &i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const JobExecWaitSlotBlameByKind = `-- name: JobExecWaitSlotBlameByKind :many
WITH ov AS (
    SELECT id, kind, GREATEST(attempted_at, $1::timestamptz) AS start_t, LEAST(finalized_at, $2::timestamptz) AS end_t
    FROM job_exec_sample WHERE attempted_at <= $2::timestamptz AND finalized_at >= $1::timestamptz
),
endpoints AS ( SELECT start_t AS t, 1 AS delta FROM ov UNION ALL SELECT end_t, -1 FROM ov ),
netted   AS ( SELECT t, SUM(delta) AS d FROM endpoints GROUP BY t ),
running  AS ( SELECT t, SUM(d) OVER (ORDER BY t) AS c FROM netted ),
seg      AS ( SELECT t AS seg_start, LEAD(t) OVER (ORDER BY t) AS seg_end, c FROM running ),
sat      AS ( SELECT seg_start, seg_end FROM seg WHERE seg_end IS NOT NULL AND c >= $3::int ),
waiters  AS (
    SELECT id, kind, attempted_at,
           attempted_at - make_interval(secs => queue_wait_ms / 1000.0) AS eligible_at
    FROM job_exec_sample
    WHERE attempted_at >= $1::timestamptz AND attempted_at <= $2::timestamptz AND queue_wait_ms > 0
),
wait_sat AS (
    SELECT w.id AS wait_id, w.kind AS wait_kind,
           GREATEST(w.eligible_at, s.seg_start) AS iv_start,
           LEAST(w.attempted_at,  s.seg_end)    AS iv_end
    FROM waiters w
    JOIN sat s ON s.seg_start < w.attempted_at AND s.seg_end > w.eligible_at
),
blame AS (
    SELECT ws.wait_kind, r.kind AS running_kind,
           EXTRACT(EPOCH FROM (LEAST(ws.iv_end, r.end_t) - GREATEST(ws.iv_start, r.start_t))) AS secs
    FROM wait_sat ws
    JOIN ov r ON r.id <> ws.wait_id AND r.start_t < ws.iv_end AND r.end_t > ws.iv_start
)
SELECT wait_kind, running_kind, SUM(secs)::float8 AS blame_slot_s
FROM blame WHERE secs > 0
GROUP BY wait_kind, running_kind
ORDER BY wait_kind, blame_slot_s DESC
`

type JobExecWaitSlotBlameByKindParams struct {
	WindowStart pgtype.Timestamptz `json:"window_start"`
	WindowEnd   pgtype.Timestamptz `json:"window_end"`
	Threshold   int32              `json:"threshold"`
}

type JobExecWaitSlotBlameByKindRow struct {
	WaitKind    string  `json:"wait_kind"`
	RunningKind string  `json:"running_kind"`
	BlameSlotS  float64 `json:"blame_slot_s"`
}

// THE decisive metric-2 query: duration-weighted "who held the pool WHILE a
// given kind was waiting during saturation." For each waiter, intersect its wait
// window [eligible_at, attempted_at] with the saturated segments; then, over
// exactly those intersections, attribute slot-seconds to each OTHER running
// job's kind. Grouped by (wait_kind, running_kind), so a consumer kind's blame
// mix is scoped to ITS OWN saturated waits — a different saturated period
// dominated by other kinds cannot bleed into it.
func (q *Queries) JobExecWaitSlotBlameByKind(ctx context.Context, arg JobExecWaitSlotBlameByKindParams) ([]*JobExecWaitSlotBlameByKindRow, error) {
	rows, err := q.db.Query(ctx, JobExecWaitSlotBlameByKind, arg.WindowStart, arg.WindowEnd, arg.Threshold)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []*JobExecWaitSlotBlameByKindRow{}
	for rows.Next() {
		var i JobExecWaitSlotBlameByKindRow
		if err := rows.Scan(&i.WaitKind, &i.RunningKind, &i.BlameSlotS); err != nil {
			return nil, err
		}
		items = append(items, &i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const ListJobExecSamplesByRiverJobIDForTest = `-- name: ListJobExecSamplesByRiverJobIDForTest :many
SELECT river_job_id, kind, queue, attempted_at, finalized_at, attempt, state, queue_wait_ms, created_at
FROM job_exec_sample WHERE river_job_id = ANY($1::bigint[]) ORDER BY river_job_id, attempt
`

type ListJobExecSamplesByRiverJobIDForTestRow struct {
	RiverJobID  int64              `json:"river_job_id"`
	Kind        string             `json:"kind"`
	Queue       string             `json:"queue"`
	AttemptedAt pgtype.Timestamptz `json:"attempted_at"`
	FinalizedAt pgtype.Timestamptz `json:"finalized_at"`
	Attempt     int32              `json:"attempt"`
	State       string             `json:"state"`
	QueueWaitMs int64              `json:"queue_wait_ms"`
	CreatedAt   pgtype.Timestamptz `json:"created_at"`
}

// Test-only read of sample rows by river_job_id (raw SQL is banned in Go tests,
// so the real-Subscribe integration test reads rows back through this query).
func (q *Queries) ListJobExecSamplesByRiverJobIDForTest(ctx context.Context, riverJobIds []int64) ([]*ListJobExecSamplesByRiverJobIDForTestRow, error) {
	rows, err := q.db.Query(ctx, ListJobExecSamplesByRiverJobIDForTest, riverJobIds)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []*ListJobExecSamplesByRiverJobIDForTestRow{}
	for rows.Next() {
		var i ListJobExecSamplesByRiverJobIDForTestRow
		if err := rows.Scan(
			&i.RiverJobID,
			&i.Kind,
			&i.Queue,
			&i.AttemptedAt,
			&i.FinalizedAt,
			&i.Attempt,
			&i.State,
			&i.QueueWaitMs,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, &i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const Tier0RiverJobStatsByKind = `-- name: Tier0RiverJobStatsByKind :many
SELECT kind,
       COUNT(*)::bigint AS n,
       percentile_cont(0.5) WITHIN GROUP (ORDER BY EXTRACT(EPOCH FROM (attempted_at - scheduled_at)))::float8 AS p50_wait_s,
       percentile_cont(0.5) WITHIN GROUP (ORDER BY EXTRACT(EPOCH FROM (finalized_at  - attempted_at)))::float8 AS p50_run_s
FROM river_job
WHERE finalized_at IS NOT NULL
  -- attempted_at is nullable: a job cancelled/discarded BEFORE it ran is
  -- finalized but never attempted. Without this guard its NULL attempted_at
  -- yields NULL percentiles, and the ::float8 cast makes sqlc scan into a
  -- non-nullable float64 → the whole read aborts. Exclude never-run rows.
  AND attempted_at IS NOT NULL
  AND finalized_at > $1::timestamptz
GROUP BY kind
ORDER BY kind
`

type Tier0RiverJobStatsByKindRow struct {
	Kind     string  `json:"kind"`
	N        int64   `json:"n"`
	P50WaitS float64 `json:"p50_wait_s"`
	P50RunS  float64 `json:"p50_run_s"`
}

// One-shot wait/run-by-kind read over river_job rows finalized since @cutoff,
// before job_exec_sample has accrued. finalized_at is authoritative here (only
// finished rows have it); this reads the live River table, not our sample table.
// APPROXIMATE: wait here is attempted_at - scheduled_at over the LAST attempt
// only, and River mutates scheduled_at on retry, so for retried jobs this
// under/over-states the true available-wait. It is a rough first signal ONLY; do
// NOT compare it numerically to the Tier-1 queue_wait_ms metric, which is
// River's exact QueueWaitDuration.
func (q *Queries) Tier0RiverJobStatsByKind(ctx context.Context, cutoff pgtype.Timestamptz) ([]*Tier0RiverJobStatsByKindRow, error) {
	rows, err := q.db.Query(ctx, Tier0RiverJobStatsByKind, cutoff)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []*Tier0RiverJobStatsByKindRow{}
	for rows.Next() {
		var i Tier0RiverJobStatsByKindRow
		if err := rows.Scan(
			&i.Kind,
			&i.N,
			&i.P50WaitS,
			&i.P50RunS,
		); err != nil {
			return nil, err
		}
		items = append(items, &i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const TrimJobExecSamples = `-- name: TrimJobExecSamples :execrows
DELETE FROM job_exec_sample WHERE created_at < $1::timestamptz
`

// Housekeeping DELETE. Cutoff is accelerated-now minus the retention window,
// computed by the caller (NOT SQL NOW()).
func (q *Queries) TrimJobExecSamples(ctx context.Context, cutoff pgtype.Timestamptz) (int64, error) {
	result, err := q.db.Exec(ctx, TrimJobExecSamples, cutoff)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
